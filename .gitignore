# ══════════════════════════════════════════════════════════════════
#  KHÔNG BAO GIỜ ĐẨY LÊN — dữ liệu đăng nhập & dữ liệu chạy
#  (profile chứa cookie đăng nhập TikTok của tài khoản thật)
# ══════════════════════════════════════════════════════════════════
profiles/
config/
logs/
session.state.json
session.state.json.bak
session.good.json
fingerprint.json
profile.lock

# Kho nén profile — cũng chứa cookie đăng nhập
*.rar
*.zip
*.7z

# Kho lọc trùng cục bộ (QĐ-36) — dữ liệu chạy, KHÔNG phải mã nguồn.
# Repo này PUBLIC: file thật của người dùng đang có 208.060 link (13,3 MB), không đẩy lên.
# Không dùng dấu / ở đầu — file này nằm cạnh .exe nên mỗi máy một chỗ khác nhau
# (Crawl_DataTiktok_Release/ khi đóng gói, Crawl_DataTiktok_build/ khi chạy dev, gốc repo nếu
# người dùng tự để đó). Mẫu không có / khớp ở MỌI cấp.
known_links.txt

# Khóa/bí mật
*.pem
*.key
.env
.env.*
!.env.example
service-account*.json
*credentials*.json

# ══════════════════════════════════════════════════════════════════
#  Thư mục triển khai (2GB: app đã đóng gói + trình duyệt + profile)
# ══════════════════════════════════════════════════════════════════
Crawl_DataTiktok_Release/

# ══════════════════════════════════════════════════════════════════
#  Nặng / tự sinh lại được
# ══════════════════════════════════════════════════════════════════
node_modules/
lib/ms-playwright/
dist/
out/
electron-builder/
*.log

# OS / IDE
Thumbs.db
.DS_Store
Desktop.ini
.vscode/
.idea/
*.swp

# Ảnh chụp layout do test/ui-responsive.test.js sinh ra (chạy lại là có)
.ui-shots/
